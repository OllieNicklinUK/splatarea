# tankarena-3d

Source-of-truth workspace for the 3D tank arena game before template extraction.

Layout:

- `app/`: runnable source project
- `docs/`: design notes, publish notes, integration plans
- `export/`: future template export scripts and metadata

This source should be validated in publish/test loops before creating
`templates/tankarena-3d-v1`.
