# Export

This directory is reserved for the future template extraction flow.

Planned contents:

- `build-template.mjs`
- exported `template.json`
- exported `TEMPLATE.md`

Do not point the agent at this source tree directly as a certified template until
the export path is implemented and validated.
